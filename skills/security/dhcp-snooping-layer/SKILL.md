---
name: dhcp-snooping-layer
description: "John Hammond videosundan çıkarılan: a DHCP snooping is a layer 2 security a DHCP snooping is a layer 2 security a DHCP snooping is a lay"
version: 1.0
author: hermes
category: security
source: "https://youtu.be/OVip4bNVMfI"
tags: [, david-bombal, video-notu]
---

# a DHCP snooping is a layer 2 security a DHCP snooping is a layer 2 security a DHCP snooping is a lay

**Kaynak:** [YouTube](https://youtu.be/OVip4bNVMfI) | **Kanal:** david-bombal
**Kategori:** ai-security

---

## 🎯 Konu

Bu videoda şu konu anlatılıyor: siber güvenlik

---

## 📝 Transcript Özeti

a DHCP snooping is a layer 2 security a DHCP snooping is a layer 2 security a DHCP snooping is a layer 2 security feature that helps you implement a feature that helps you implement a feature that helps you implement a better security in an Ethernet switch to better security in an Ethernet switch to better security in an Ethernet switch to environment it's once again a layer 2 environment it's once again a layer 2 environment it's once again a layer 2 feature and not a layer 3 or router feature and not a layer 3 or router feature and not a layer 3 or router feature that validates a DHCP messages feature that validates a DHCP messages feature that validates a DHCP messages it allows you to create what are called it allows you to create what are called it allows you to create what are called trusted and untrusted ports and allows trusted and untrusted ports and allows trusted and untrusted ports and allows you to prevent rogue DHCP servers you to prevent rogue DHCP servers you to prevent rogue DHCP servers sending DHCP offers to clients it sending DHCP offers to clients it sending DHCP offers to clients it essentially looks for DHCP messages and essentially looks for DHCP messages and essentially looks for DHCP messages and ignores non DHCP messages but validates ignores non DHCP messages but validates ignores non DHCP messages but validates and ensures that a DHCP offer messages and ensures that a DHCP offer messages and ensures that a DHCP offer messages are only received on trusted ports if a are only received on trusted ports if a are only received on trusted ports if a rogue DHCP server was configured on an rogue DHCP server was configured on an rogue DHCP server was configured on an untrusted port the offer from that a untrusted port the offer from that a untrusted port the offer from that a DHCP server would not be forwarded to DHCP server would not be forwarded to DHCP server would not be forwarded to client pcs requesting IP addresses this client pcs requesti

---

## 🔑 Anahtar Kelimeler
Siber güvenlik

---

## 📌 Nasıl Kullanılır
1. Videoyu izle: https://youtu.be/OVip4bNVMfI
2. Yukarıdaki transcript özetini oku
3. Konuyu pratikte dene (güvenli ortamda)

---

## 🔗 İlgili Skill'ler
- john-hammond-siber-guvenlik-arsivi
