---
name: payload
description: "John Hammond videosundan çıkarılan: now before we configure our tunnels we now before we configure our tunnels we now before we configur"
version: 1.0
author: hermes
category: security
source: "https://youtu.be/_NZHzIdXuss"
tags: [payload, david-bombal, video-notu]
---

# now before we configure our tunnels we now before we configure our tunnels we now before we configur

**Kaynak:** [YouTube](https://youtu.be/_NZHzIdXuss) | **Kanal:** david-bombal
**Kategori:** general-security

---

## 🎯 Konu

Bu videoda şu konu anlatılıyor: payload

---

## 📝 Transcript Özeti

now before we configure our tunnels we now before we configure our tunnels we now before we configure our tunnels we need to make sure that the tunnel need to make sure that the tunnel need to make sure that the tunnel endpoints have connectivity to each endpoints have connectivity to each endpoints have connectivity to each other so router two should be able to other so router two should be able to other so router two should be able to ping route a four and vice versa in this ping route a four and vice versa in this ping route a four and vice versa in this example I'm going to setup the tunnel example I'm going to setup the tunnel example I'm going to setup the tunnel from these serial interfaces so from these serial interfaces so from these serial interfaces so logically it looks like we have this logically it looks like we have this logically it looks like we have this separate tunnel interface but the tunnel separate tunnel interface but the tunnel separate tunnel interface but the tunnel is actually configured from serial zero is actually configured from serial zero is actually configured from serial zero two zero two serial zero two zero so two zero two serial zero two zero so two zero two serial zero two zero so router - can we paying for two one two router - can we paying for two one two router - can we paying for two one two yes we can and on rata for two yes we can and on rata for two yes we can and on rata for can we paying for one two one two one can we paying for one two one two one can we paying for one two one two one yes we can now ping uses a different yes we can now ping uses a different yes we can now ping uses a different protocol to GRE so you need to confirm protocol to GRE so you need to confirm protocol to GRE so you need to confirm that you have connectivity using GRE that you have connectivity using GRE that you have connectivity using GRE from this interface to this interface from this interface to this interface from this interface to thi

---

## 🔑 Anahtar Kelimeler
payload

---

## 📌 Nasıl Kullanılır
1. Videoyu izle: https://youtu.be/_NZHzIdXuss
2. Yukarıdaki transcript özetini oku
3. Konuyu pratikte dene (güvenli ortamda)

---

## 🔗 İlgili Skill'ler
- john-hammond-siber-guvenlik-arsivi
