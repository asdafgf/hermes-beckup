---
name: python-flask-web-servers-can-left
description: "python flask web servers can be left in python flask web servers can be left in "
version: 1.0
category: security
source: "https://youtu.be/jwBRgaIRdgs"
tags: [python, ai, john-hammond]
---

# python flask web servers can be left in python flask web servers can be left in 

**Kaynak:** [YouTube](https://youtu.be/jwBRgaIRdgs) | **Kanal:** john-hammond

---

## 📝 Transcript Özeti

python flask web servers can be left in python flask web servers can be left in python flask web servers can be left in a configuration State called debug mode a configuration State called debug mode a configuration State called debug mode where developers can access variables or where developers can access variables or where developers can access variables or see code running in the application and see code running in the application and see code running in the application and even access an interactive console where even access an interactive console where even access an interactive console where they can run commands with the python they can run commands with the python they can run commands with the python interpreter now if this is left on for interpreter now if this is left on for interpreter now if this is left on for an application in production it can be an application in production it can be an application in production it can be very bad because an adversary or throat very bad because an adversary or throat very bad because an adversary or throat actor could compromise that application actor could compromise that application actor could compromise that application thankfully this is sort of mitigated in thankfully this is sort of mitigated in thankfully this is sort of mitigated in that you can actually enable a pin or that you can actually enable a pin or that you can actually enable a pin or identifying number that will only allow identifying number that will only allow identifying number that will only allow access to the console If someone knows access to the console If someone knows access to the console If someone knows it and this is normally just shown to it and this is normally just shown to it and this is normally just shown to the developer within the console when the developer within the console when the developer within the console when they spin up their application but under they spin up their application but under they spin up their applic

---

## 🔑 Anahtar Kelimeler
python, ai

---

