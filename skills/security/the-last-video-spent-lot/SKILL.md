---
name: the-last-video-spent-lot
description: "in the last video we spent a lot of time in the last video we spent a lot of time in the last video "
version: 1.0
category: security
source: "https://youtu.be/WhBJrWUb57o"
tags: [ai, john-hammond]
---

# in the last video we spent a lot of time in the last video we spent a lot of tim

**Kaynak:** [YouTube](https://youtu.be/WhBJrWUb57o) | **Kanal:** john-hammond

---

## 📝 Transcript Özeti

in the last video we spent a lot of time in the last video we spent a lot of time in the last video we spent a lot of time playing with powershell to be able to playing with powershell to be able to playing with powershell to be able to create a local administrator on a host create a local administrator on a host create a local administrator on a host within our domain but it only did it on within our domain but it only did it on within our domain but it only did it on the domain controller because that's all the domain controller because that's all the domain controller because that's all that our powershell script to generate that our powershell script to generate that our powershell script to generate this active directory environment was this active directory environment was this active directory environment was really running on it would be very cool really running on it would be very cool really running on it would be very cool if we were able to create local admins if we were able to create local admins if we were able to create local admins across different workstations or other across different workstations or other across different workstations or other servers within our environment but that servers within our environment but that servers within our environment but that might take a little bit of tinkering and might take a little bit of tinkering and might take a little bit of tinkering and finagling uh with that said i realized finagling uh with that said i realized finagling uh with that said i realized that setting up this environment is that setting up this environment is that setting up this environment is totally for the sake of creating a totally for the sake of creating a totally for the sake of creating a vulnerable active director environment vulnerable active director environment vulnerable active director environment so the stuff that we do within this so the stuff that we do within this so the stuff that we do within this video might not be c

---

## 🔑 Anahtar Kelimeler
ai

---
