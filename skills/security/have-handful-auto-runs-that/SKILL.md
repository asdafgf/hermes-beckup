---
name: have-handful-auto-runs-that
description: "I have a handful of Auto runs that have I have a handful of Auto runs that have I have a handful of "
version: 1.0
category: security
source: "https://youtu.be/1-FyyhpW-t8"
tags: [hack, malware, windows, john-hammond]
---

# I have a handful of Auto runs that have I have a handful of Auto runs that have 

**Kaynak:** [YouTube](https://youtu.be/1-FyyhpW-t8) | **Kanal:** john-hammond

---

## 📝 Transcript Özeti

I have a handful of Auto runs that have I have a handful of Auto runs that have I have a handful of Auto runs that have been set up and installed by malware and been set up and installed by malware and been set up and installed by malware and I mean auto run in that it will I mean auto run in that it will I mean auto run in that it will automatically run and execute an invoke automatically run and execute an invoke automatically run and execute an invoke code just as the user logs in or the code just as the user logs in or the code just as the user logs in or the computer starts up and it's set up as a computer starts up and it's set up as a computer starts up and it's set up as a persistence mechanism for thread actors persistence mechanism for thread actors persistence mechanism for thread actors adversaries hackers and malware now this adversaries hackers and malware now this adversaries hackers and malware now this is the syntax in fact I have each of is the syntax in fact I have each of is the syntax in fact I have each of these on different lines these are all these on different lines these are all these on different lines these are all different Auto runs that have been set different Auto runs that have been set different Auto runs that have been set up and staged on different compromised up and staged on different compromised up and staged on different compromised windows hosts each individual endpoint windows hosts each individual endpoint windows hosts each individual endpoint had their own unique little Stager here had their own unique little Stager here had their own unique little Stager here set up as a scheduled task or service set up as a scheduled task or service set up as a scheduled task or service however they might set up this hook and however they might set up this hook and however they might set up this hook and Claw for an auto run and persistence let Claw for an auto run and persistence let Claw for an auto run and persistence let me grab one

---

## 🔑 Anahtar Kelimeler
hack, malware, windows

---
