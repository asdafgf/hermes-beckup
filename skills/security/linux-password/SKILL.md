---
name: linux-password
description: "John Hammond videosundan çıkarılan: so I'll log in default username is so I'll log in default username is so I'll log in default usernam"
version: 1.0
author: hermes
category: security
source: "https://youtu.be/T82pgDm2ul4"
tags: [linux, password, david-bombal, video-notu]
---

# so I'll log in default username is so I'll log in default username is so I'll log in default usernam

**Kaynak:** [YouTube](https://youtu.be/T82pgDm2ul4) | **Kanal:** david-bombal
**Kategori:** linux-security

---

## 🎯 Konu

Bu videoda şu konu anlatılıyor: linux, password

---

## 📝 Transcript Özeti

so I'll log in default username is so I'll log in default username is so I'll log in default username is cumulus default password is cumulus cumulus default password is cumulus cumulus default password is cumulus Linux bang if config shows me that by default bang if config shows me that by default bang if config shows me that by default I only have eth zero the management port I only have eth zero the management port I only have eth zero the management port and loop back zero and loop back zero and loop back zero configured log to switch 2 I have config shows me the same 2 I have config shows me the same 2 I have config shows me the same information eth0 and loopback z are information eth0 and loopback z are information eth0 and loopback z are config so what I'll do initially is configure so what I'll do initially is configure so what I'll do initially is configure the interfaces on the switch and get the the interfaces on the switch and get the the interfaces on the switch and get the two pieces to Ping each other in VLAN two pieces to Ping each other in VLAN two pieces to Ping each other in VLAN one and then we'll move them to a one and then we'll move them to a one and then we'll move them to a separate VLAN this is Linux so you use separate VLAN this is Linux so you use separate VLAN this is Linux so you use Linux commands such as Nano to edit Etsy Linux commands such as Nano to edit Etsy Linux commands such as Nano to edit Etsy network interfaces so pseudo Nano ety network interfaces so pseudo Nano ety network interfaces so pseudo Nano ety network interfaces scrolling down I can see auto interfaces scrolling down I can see auto interfaces scrolling down I can see auto loop back zero and auto e loop back zero and auto e loop back zero and auto e zero so what I'll do now is create zero so what I'll do now is create zero so what I'll do now is create switch Port switch Port switch Port one I face switch Port one I face switch Port one I face switch Port one switch

---

## 🔑 Anahtar Kelimeler
linux, password

---

## 📌 Nasıl Kullanılır
1. Videoyu izle: https://youtu.be/T82pgDm2ul4
2. Yukarıdaki transcript özetini oku
3. Konuyu pratikte dene (güvenli ortamda)

---

## 🔗 İlgili Skill'ler
- john-hammond-siber-guvenlik-arsivi
