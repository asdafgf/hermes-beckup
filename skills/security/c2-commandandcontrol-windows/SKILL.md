---
name: c2-commandandcontrol-windows
description: "John Hammond videosundan çıkarılan: command and control infrastructure that command and control infrastructure that command and control "
version: 1.0
author: hermes
category: security
source: "https://youtu.be/FqZIm6vP7XM"
tags: [c2, command and control, windows, edr, john-hammond, video-notu]
---

# command and control infrastructure that command and control infrastructure that command and control 

**Kaynak:** [YouTube](https://youtu.be/FqZIm6vP7XM) | **Kanal:** john-hammond
**Kategori:** windows-security

---

## 🎯 Konu

Bu videoda şu konu anlatılıyor: c2, command and control, windows, edr

---

## 📝 Transcript Özeti

command and control infrastructure that command and control infrastructure that command and control infrastructure that allows red teamers to execute system allows red teamers to execute system allows red teamers to execute system commands on compromise hosts through commands on compromise hosts through commands on compromise hosts through Microsoft teams this is convo C2 a tool and teams this is convo C2 a tool and teams this is convo C2 a tool and project recently released I think about project recently released I think about project recently released I think about November of 2024 that is literally a November of 2024 that is literally a November of 2024 that is literally a command and control Channel over command and control Channel over command and control Channel over Microsoft teams it's actually really Microsoft teams it's actually really Microsoft teams it's actually really interesting how this works it interesting how this works it interesting how this works it infiltrates data into hidden span tags infiltrates data into hidden span tags infiltrates data into hidden span tags in Microsoft teams and exfiltrate in Microsoft teams and exfiltrate in Microsoft teams and exfiltrate command outputs in a captive card images command outputs in a captive card images command outputs in a captive card images URLs triggering out-of-bound requests to URLs triggering out-of-bound requests to URLs triggering out-of-bound requests to a command and control server this means a command and control server this means a command and control server this means there is no direct communication between there is no direct communication between there is no direct communication between the victim and the attacker and the the victim and the attacker and the the victim and the attacker and the victim only sends messages or HTTP victim only sends messages or HTTP victim only sends messages or HTTP requests to Microsoft online Microsoft requests to Microsoft online Microsoft requests to Micr

---

## 🔑 Anahtar Kelimeler
c2, command and control, windows, edr

---

## 📌 Nasıl Kullanılır
1. Videoyu izle: https://youtu.be/FqZIm6vP7XM
2. Yukarıdaki transcript özetini oku
3. Konuyu pratikte dene (güvenli ortamda)

---

## 🔗 İlgili Skill'ler
- john-hammond-siber-guvenlik-arsivi
